import Sentari from "@/components/Sentari";

export const metadata = {
  title: "Sentari — Winstone",
  description: "Civic safety as infrastructure — early-stage thesis, building in public.",
};

export default function SentariPage() {
  return (
    <main className="flex-1">
      <Sentari />
    </main>
  );
}
