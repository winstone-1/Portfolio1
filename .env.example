# WhatsApp — digits only, no + / spaces (e.g. 254795278996)
NEXT_PUBLIC_WHATSAPP_NUMBER=254795278996
# also accepted: WHATSAPP_NUMBER=254795278996

# Groq — server-only, never expose to client
GROQ_API_KEY=your_groq_api_key_here
# optional override: GROQ_MODEL=llama-3.3-70b-versatile
